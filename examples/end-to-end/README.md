# README

## Cypress coverage modes

1. Dev server mode (existing tests):
   - `npm start`
   - `npx cypress run`

2. Netlify build mode (frozen views + serverless runtime):
   - `npm run test:netlify:frozen-views`
